---
# Sensitive data - Encrypt this file with ansible-vault
# Usage: ansible-vault encrypt vars/secrets.yml

# NVIDIA / MOK Settings
mok_password: "fedora"

# Gemini CLI / API Keys
# Add your keys here and they will be injected into .zshrc in Stage 1
api_keys: |
  # export GEMINI_API_KEY="your-key-here"
  # export GOOGLE_API_KEY="your-key-here"
